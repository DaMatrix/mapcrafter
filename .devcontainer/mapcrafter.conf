server {
    listen 80;
    # server_name maps.pcn.pouille.me;

	access_log /var/log/nginx/access.log combined;
	error_log /var/log/nginx/error.log warn;

    # Make sure all types are defined
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    sendfile        on;
    keepalive_timeout  60;

    # certificate file
    # ssl_certificate      certs/maps.pcn.pouille.me/fullchain.pem;
    # ssl_certificate_key  certs/maps.pcn.pouille.me/privkey.pem;

    # Prevent nginx HTTP Server Detection
    server_tokens off;

    # # HSTS settings
    # # WARNING: Only add the preload option once you read about
    # # the consequences in https://hstspreload.org/. This option
    # # will add the domain to a hardcoded list that is shipped
    # # in all major browsers and getting removed from this list
    # # could take several months.
    # add_header Strict-Transport-Security "max-age=15552000; includeSubDomains; preload;" always;

    # Enable gzip but do not remove ETag headers
    gzip on;
    gzip_vary on;
    gzip_comp_level 4;
    gzip_min_length 256;
    gzip_proxied expired no-cache no-store private no_last_modified no_etag auth;
    gzip_types application/atom+xml application/javascript application/json application/ld+json application/manifest+json application/rss+xml application/vnd.geo+json application/vnd.ms-fontobject application/wasm application/x-font-ttf application/x-web-app-manifest+json application/xhtml+xml application/xml font/opentype image/bmp image/svg+xml image/x-icon text/cache-manifest text/css text/plain text/vcard text/vnd.rim.location.xloc text/vtt text/x-component text/x-cross-domain-policy;

    # The settings allows you to optimize the HTTP2 bandwitdth.
    # See https://blog.cloudflare.com/delivering-http-2-upload-speed-improvements/
    # for tunning hints
    client_body_buffer_size 512k;

    # HTTP response headers borrowed from Nextcloud `.htaccess`
    add_header Referrer-Policy                      "no-referrer"   always;
    add_header X-Content-Type-Options               "nosniff"       always;
    add_header X-Download-Options                   "noopen"        always;
    add_header X-Frame-Options                      "SAMEORIGIN"    always;
    add_header X-Permitted-Cross-Domain-Policies    "none"          always;
    add_header X-Robots-Tag                         "none"          always;
    add_header X-XSS-Protection                     "1; mode=block" always;

    location / {
        index index.html;
        root /var/www/mchtml;

        try_files $uri $uri/ /index.html;

        # set cache
        add_header Cache-Control 'no-cache, must-revalidate, proxy-revalidate, no-transform';
        if_modified_since before;
        expires 14d;
        etag on;
    }

}
